@model IEnumerable<DVDCollection.Models.FilmCertificate>

@{
    ViewData["Title"] = "Film Certificates";
}

<h1>Film Certificates</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<form asp-action="Index" method="get">
    <div class="form-actions no-color">
        <p>
            Find by Certificate: <input type="text" name="SearchString"  />
            <input type="submit" value="Search" /> |
            <a asp-action="Index">Back to Full List</a>
        </p>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Certificate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Certificate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CertificateID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CertificateID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CertificateID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
